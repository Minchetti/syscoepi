import React from 'react';
import ReactDOM from 'react-dom';
import TableEpis2 from './TableEpis2';

it('renders without crashing', () => {
  const div = document.createElement('div');
  ReactDOM.render(<TableEpis2 />, div);
});